version: "3.8"

services:
  web:
    container_name: sayr-web
    image: ghcr.io/dorasto/sayr-web:latest
    restart: always
    pull_policy: always
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - DORAS_CLIENT_ID=${DORAS_CLIENT_ID}
      - DORAS_CLIENT_SECRET=${DORAS_CLIENT_SECRET}
      - NEXT_PUBLIC_URL_ROOT=${NEXT_PUBLIC_URL_ROOT}
      - NEXT_PUBLIC_API_SERVER=${NEXT_PUBLIC_API_SERVER}
      - NEXT_PUBLIC_PROJECT_NAME=${NEXT_PUBLIC_PROJECT_NAME}
      - NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL}
      - NEXT_PUBLIC_ROOT_DOMAIN=${NEXT_PUBLIC_ROOT_DOMAIN}
      - NEXT_PUBLIC_EXTERNAL_API_URL=${NEXT_PUBLIC_EXTERNAL_API_URL}
      - STORAGE_URL=${STORAGE_URL}
      - STORAGE_BUCKET=${STORAGE_BUCKET}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY}
      - FILE_SALT=${FILE_SALT}
      - FILE_CDN=${FILE_CDN}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
      - GITHUB_APP_ID=${GITHUB_APP_ID}
      - GITHUB_APP_PRIVATE_KEY=${GITHUB_APP_PRIVATE_KEY}
      - GITHUB_WEBHOOK_SECRET=${GITHUB_WEBHOOK_SECRET}
      - STORAGE_PORT=${STORAGE_PORT}
      - FILE_NAME_HASH_LENGTH=${FILE_NAME_HASH_LENGTH}
      - DORAS_ORGANIZATION=${DORAS_ORGANIZATION}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - NEXT_PUBLIC_SAYR_FRONTEND_AXIOM_TOKEN=${NEXT_PUBLIC_SAYR_FRONTEND_AXIOM_TOKEN}
      - NEXT_PUBLIC_SAYR_FRONTEND_AXIOM_DATASET=${NEXT_PUBLIC_SAYR_FRONTEND_AXIOM_DATASET}
    networks:
      - app_network

  backend:
    container_name: sayr-backend
    image: ghcr.io/dorasto/sayr-backend:latest
    restart: always
    pull_policy: always
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - DORAS_CLIENT_ID=${DORAS_CLIENT_ID}
      - DORAS_CLIENT_SECRET=${DORAS_CLIENT_SECRET}
      - NEXT_PUBLIC_URL_ROOT=${NEXT_PUBLIC_URL_ROOT}
      - NEXT_PUBLIC_API_SERVER=${NEXT_PUBLIC_API_SERVER}
      - NEXT_PUBLIC_PROJECT_NAME=${NEXT_PUBLIC_PROJECT_NAME}
      - STORAGE_URL=${STORAGE_URL}
      - STORAGE_BUCKET=${STORAGE_BUCKET}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY}
      - FILE_SALT=${FILE_SALT}
      - FILE_CDN=${FILE_CDN}
      - SAYR_EMAIL=${SAYR_EMAIL}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
      - GITHUB_APP_ID=${GITHUB_APP_ID}
      - GITHUB_APP_PRIVATE_KEY=${GITHUB_APP_PRIVATE_KEY}
      - GITHUB_WEBHOOK_SECRET=${GITHUB_WEBHOOK_SECRET}
      - STORAGE_PORT=${STORAGE_PORT}
      - FILE_NAME_HASH_LENGTH=${FILE_NAME_HASH_LENGTH}
      - DORAS_ORGANIZATION=${DORAS_ORGANIZATION}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - QUEUE_MODE=${QUEUE_MODE}
      - REDIS_URL=${REDIS_URL}
      - INTERNAL_SECRET=${INTERNAL_SECRET}
      - SYSTEM_ACCOUNT_ID=${SYSTEM_ACCOUNT_ID}
      - AXIOM_BACKEND_OTEL_DOMAIN=${AXIOM_BACKEND_OTEL_DOMAIN}
      - AXIOM_BACKEND_OTEL_TOKEN=${AXIOM_BACKEND_OTEL_TOKEN}
      - AXIOM_BACKEND_OTEL_DATASET=${AXIOM_BACKEND_OTEL_DATASET}
    networks:
      - app_network

  github-worker:
    container_name: sayr-worker-github
    image: ghcr.io/dorasto/sayr-worker:latest
    restart: always
    pull_policy: always
    command: ["bun", "run", "dist/index.js", "github"]
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - DORAS_CLIENT_ID=${DORAS_CLIENT_ID}
      - DORAS_CLIENT_SECRET=${DORAS_CLIENT_SECRET}
      - NEXT_PUBLIC_URL_ROOT=${NEXT_PUBLIC_URL_ROOT}
      - NEXT_PUBLIC_API_SERVER=${NEXT_PUBLIC_API_SERVER}
      - NEXT_PUBLIC_PROJECT_NAME=${NEXT_PUBLIC_PROJECT_NAME}
      - STORAGE_URL=${STORAGE_URL}
      - STORAGE_BUCKET=${STORAGE_BUCKET}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY}
      - FILE_SALT=${FILE_SALT}
      - FILE_CDN=${FILE_CDN}
      - SAYR_EMAIL=${SAYR_EMAIL}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
      - GITHUB_APP_ID=${GITHUB_APP_ID}
      - GITHUB_APP_PRIVATE_KEY=${GITHUB_APP_PRIVATE_KEY}
      - GITHUB_WEBHOOK_SECRET=${GITHUB_WEBHOOK_SECRET}
      - STORAGE_PORT=${STORAGE_PORT}
      - FILE_NAME_HASH_LENGTH=${FILE_NAME_HASH_LENGTH}
      - DORAS_ORGANIZATION=${DORAS_ORGANIZATION}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - QUEUE_MODE=${QUEUE_MODE}
      - REDIS_URL=${REDIS_URL}
      - INTERNAL_SECRET=${INTERNAL_SECRET}
      - SYSTEM_ACCOUNT_ID=${SYSTEM_ACCOUNT_ID}
      - AXIOM_BACKEND_OTEL_DOMAIN=${AXIOM_BACKEND_OTEL_DOMAIN}
      - AXIOM_BACKEND_OTEL_TOKEN=${AXIOM_BACKEND_OTEL_TOKEN}
      - AXIOM_BACKEND_OTEL_DATASET=${AXIOM_BACKEND_OTEL_DATASET}
    networks:
      - app_network

  redis:
    image: redis:7-alpine
    container_name: sayr-redis
    restart: always
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    networks:
      - app_network

  nginx:
    build:
      context: .
      dockerfile: ./Dockerfile.nginx
    container_name: sayr-nginx
    ports:
      - "3000:80"
    depends_on:
      - web
      - backend
    networks:
      - app_network

networks:
  app_network:
    driver: bridge

volumes:
  redis_data:
